services:
  db:
    image: postgres:15.6
    container_name: todo-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "${DB_DATABASE}"
      POSTGRES_USER: "${DB_USERNAME}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:-secret}"
      PGDATA: /var/lib/postgresql/data
    volumes:
      - todo-postgres:/var/lib/postgresql/data
    networks:
      - todo-backend
    env_file:
      - .env
    healthcheck:
      test:
        [
          "CMD",
          "pg_isready",
          "-q",
          "-d",
          "${DB_DATABASE}",
          "-U",
          "${DB_USERNAME}",
        ]
      interval: 10s
      retries: 3
      timeout: 5s
    restart: unless-stopped

  adminer:
    image: adminer
    container_name: todo-adminer
    ports:
      - "8080:8080"
    networks:
      - todo-backend
    labels:
      - "traefik.http.routers.adminer.rule=Host(`db.tersucorp.com`)"
    depends_on:
      db:
        condition: service_healthy

volumes:
  todo-postgres:
    driver: local
